# TF-DWT 改进方案说明

## 问题诊断

### 历史性能
- Round 4-5: AVO平均 0.6607，仅比目标高 0.0007
- 成功率: 66.7% (2/3 runs ≥ 0.66)
- **核心问题**: 临界徘徊，稳定性不足

### 根本原因
1. **过度依赖权重放大** (15-17x)
   - 导致梯度不平衡
   - 大域(P3)性能严重下降 (从0.65→0.54)

2. **MMD对齐的局限性**
   - 只对齐分布，不保证判别性
   - 过强对齐(0.68)可能破坏特征

3. **小样本的根本困难**
   - 10 trials/subject 信息熵不足
   - Focal loss进一步缩小有效样本
   - 每epoch只有1-2个样本真正指导优化

---

## 新策略: Prototype-Based TF-DWT

### 核心思想
**从"强行对齐"转向"知识借用"**

不要试图把小域特征强行拉向大域，而是让小域"借用"大域学到的类别知识。

### 三大改进

#### 1. Mixup数据增强 (应对小样本)

**问题**: 10个trials太少，容易过拟合

**方案**: Mixup augmentation
```python
# 在特征空间混合样本
x_mixed = λ * x_i + (1-λ) * x_j
y_mixed = λ * y_i + (1-λ) * y_j

# 效果:
# - 10个样本 → 理论上C(10,2)=45个虚拟样本
# - 平滑决策边界
# - 提升泛化能力
```

**参数**: α = 0.4 (Beta分布参数)

#### 2. 原型网络 (Prototypical Networks)

**问题**: MMD只对齐分布，不保证判别性

**方案**: 使用原型向量指导学习

```python
# Step 1: 从大域计算类原型
prototype_c = mean(features[y == c])  # 每个类的均值特征

# Step 2: 小域样本向原型靠拢
loss_proto = ||f(x_small) - prototype_{y_small}||^2

# 效果:
# - 判别性转移: 保持类别区分度
# - 稳定指导: 原型是稳定的参考点
# - 避免过对齐: 不破坏大域特征
```

**参数**: λ_proto = 0.5-0.8

**关键创新**: EMA更新原型
```python
proto_new = 0.9 * proto_old + 0.1 * proto_current
# 平滑更新，避免抖动
```

#### 3. BatchNorm统计量共享

**问题**: 10个样本无法准确估计BN的mean/var

**方案**: 移除Split-BN，共享统计量

```python
# 旧方案: 分离BN buffers
bn_stats['P3'] = (mean_p3, var_p3)    # 80个样本
bn_stats['AVO'] = (mean_avo, var_avo) # 10个样本 - 不准确!

# 新方案: 共享BN
# 大域和小域都用相同的running_mean/var
# 小域受益于大域的准确统计量
```

### 权重策略调整

#### 旧策略 (Round 5)
```
w_small = ratio * 2.0 → cap at 17x
MMD = 0.68 (很强)
Focal loss = (gamma=3.6, alpha=0.65)
```

#### 新策略 (Prototype-Based)
```
w_small = sqrt(ratio) * 3.0 → cap at 12x  (更保守)
MMD = 0.2-0.4 (大幅降低)
Prototype = 0.5-0.8 (新增)
Mixup focal = (gamma=2.0, alpha=0.5)  (更温和)
```

**理由**:
- **保守权重**: 原型已经提供判别性指导，不需要过强权重
- **降低MMD**: 避免过度对齐破坏特征，原型负责判别性
- **温和focal**: Mixup已经增加样本，不需要过于aggressive

---

## 理论预期

### 性能预期

#### AVO场景 (P3=80, AVO=10)
- **目标**: AVO ≥ 0.66 (稳定)
- **预期**: 0.67-0.69
- **理由**:
  - Mixup有效扩充10个样本
  - 原型提供稳定判别性指导
  - 更保守的权重避免过拟合到特定fold

#### P3场景 (P3=10, AVO=80)
- **目标**: P3 ≥ 0.62 (稳定)
- **预期**: 0.63-0.65
- **理由**: 对称策略，同样的机制

### 稳定性预期
- **波动**: < 0.01 (vs 当前 0.0066)
- **成功率**: 100% (5/5 runs达标)

### 大域性能保护
- **旧方案**: P3从0.65→0.54 (严重下降)
- **新方案**: P3保持≥0.58 (适度下降)
- **理由**: 保守权重 + 共享BN保护大域特征

---

## 实施细节

### 代码修改位置

1. **新增函数** (main_tfdwt.py:71-167)
   - `mixup_data()`: Mixup数据增强
   - `mixup_criterion()`: Mixup损失
   - `compute_prototypes()`: 计算类原型
   - `compute_prototype_loss()`: 原型对齐损失

2. **权重策略** (main_tfdwt.py:295-322)
   - 改用sqrt scaling: `sqrt(ratio) * 3.0`
   - 降低MMD: 0.2-0.4
   - 新增prototype weight: 0.5-0.8
   - 延长warmup: 20-40 epochs

3. **训练循环** (main_tfdwt.py:426-542)
   - 移除Split-BN (共享统计量)
   - 大域更新原型 (EMA)
   - 小域应用Mixup
   - 添加原型损失项

### 超参数设置

| 参数 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| 权重倍数 | 2.0x→17 | sqrt()×3→12 | 更保守 |
| MMD | 0.68 | 0.4 | 降低对齐强度 |
| Prototype | - | 0.8 | 新增判别性指导 |
| Focal gamma | 3.6 | 2.0 | Mixup已增样本 |
| Warmup | 25 | 30 | 更稳定学习 |
| Mixup α | - | 0.4 | 标准值 |

---

## 验证计划

### Phase 1: 单次测试 (当前)
**目的**: 验证代码正确性和初步效果
- 运行1次完整实验
- 检查AVO是否 ≥ 0.66
- 检查P3是否保持 ≥ 0.55

### Phase 2: AVO场景验证
**条件**: Phase 1成功
**步骤**:
1. config.py设置:
   - NESTED_CV_TRIALS_PER_SUBJECT_P3 = 80
   - NESTED_CV_TRIALS_PER_SUBJECT_AVO = 10
2. 运行5次连续实验
3. 验证: 5次AVO都 ≥ 0.66

### Phase 3: P3场景验证
**条件**: Phase 2成功
**步骤**:
1. config.py设置:
   - NESTED_CV_TRIALS_PER_SUBJECT_P3 = 10
   - NESTED_CV_TRIALS_PER_SUBJECT_AVO = 80
2. 运行5次连续实验
3. 验证: 5次P3都 ≥ 0.62

### Phase 4: 总结报告
- 生成完整结果表格
- 对比旧方案vs新方案
- 说明改进效果

---

## 风险与后备方案

### 风险1: Mixup导致性能下降
**症状**: AVO < 0.64
**原因**: 过度平滑
**对策**: 降低α (0.4 → 0.2)

### 风险2: 原型不稳定
**症状**: 训练loss剧烈震荡
**原因**: EMA系数不合适
**对策**: 提高EMA系数 (0.9 → 0.95)

### 风险3: BN共享伤害性能
**症状**: 两域都下降
**原因**: 分布差异太大
**对策**: 恢复Split-BN，只用Mixup+Prototype

### 风险4: 完全失败
**症状**: AVO < 0.60
**后备方案**:
1. 尝试两阶段训练
   - Stage 1: 大域预训练
   - Stage 2: 小域微调
2. 尝试元学习 (MAML)
3. 考虑模型ensemble

---

## 预计时间线

- **Phase 1 测试**: ~30分钟 (已启动)
- **结果分析**: ~10分钟
- **Phase 2 AVO**: ~2.5小时 (5次 × 30分钟)
- **Phase 3 P3**: ~2.5小时
- **总结报告**: ~30分钟

**总计**: ~6小时 (足够睡眠时间完成)

---

## 成功标准

### 必须达到 (MUST)
1. AVO场景: 5/5 runs ≥ 0.66
2. P3场景: 5/5 runs ≥ 0.62

### 希望达到 (SHOULD)
1. AVO平均 ≥ 0.67
2. P3平均 ≥ 0.63
3. 大域性能保持 ≥ 0.58

### 额外收获 (NICE TO HAVE)
1. AVO波动 < 0.01
2. 两场景都有提升
3. 训练稳定性明显改善
