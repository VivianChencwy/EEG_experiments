# TF-DWT实验复现指南 - 20250925_021227

本指南用于完美复现 `tfdwt_summary_stats_20250925_021227.csv` 的实验结果。

## 目标结果

复现以下准确的实验结果：
- **总体准确率**: 0.6297 ± 0.0255
- **95% 置信区间**: [0.6192, 0.6402]
- **P3数据集平均准确率**: 0.5853 | 95% CI: [0.5702, 0.6003]
- **AVO数据集平均准确率**: 0.6415 | 95% CI: [0.6298, 0.6531]

## 详细CSV目标值

| 指标 | 目标值 |
|------|--------|
| overall_accuracy_mean | 0.629708448824556 |
| overall_accuracy_std | 0.02547774544841991 |
| p3_accuracy_mean | 0.5852542485189953 |
| avo_accuracy_mean | 0.6414717435723987 |
| p3_accuracy_std | 0.03643020843794811 |
| avo_accuracy_std | 0.028288643098379484 |

## 原始实验配置

基于 `log_batch/tfdwt_combined_P3_20_AVO_200_20250925_015533.log` 提取的确切配置：

### 核心设置
```
electrode_list: all
fusion_method: none
domain_adaptation: none
use_enhanced_preprocessing: True
batch_size: 32
max_epochs: 500
learning_rate: 0.01
weight_decay: 0.0001
dropout_rate: 0.25
use_data_augmentation: True
noise_std: 0.005
time_shift_range: 5
label_smoothing: 0.05
trials_per_subject_P3: 20
trials_per_subject_AVO: 200
train/val/test: (0.7, 0.1, 0.2)
device_mode: cuda
```

### 交叉验证设置
- **外层折数**: 5-fold CV
- **重复次数**: 5 repeats
- **置信水平**: 95%
- **随机种子**: [42, 123, 456, 789, 321]

## 实验特点

此实验与20250924_204648实验的主要区别：
1. **时间戳不同**: 20250925_021227 vs 20250924_204648
2. **日志位置**: log_batch vs log_0909
3. **性能轻微差异**: 总体准确率 0.6297 vs 0.6389

配置参数完全相同，结果差异可能来自：
- 不同的随机种子序列
- 轻微的数据加载顺序差异
- 系统环境的细微变化

## 复现方法

### 方法一：自动脚本复现（推荐）

1. **运行自动复现脚本**：
```bash
python reproduce_tfdwt_20250925_021227.py
```

脚本会自动：
- 备份当前的 `config.py`
- 加载复现配置
- 运行 TF-DWT 实验
- 恢复原始配置

### 方法二：手动配置复现

1. **备份当前配置**：
```bash
cp config.py config_backup.py
```

2. **使用复现配置**：
```bash
cp config_reproduce_tfdwt_20250925_021227.py config.py
```

3. **运行实验**：
```bash
python main_tfdwt.py
```

4. **恢复原始配置**：
```bash
cp config_backup.py config.py
```

## 预期输出文件

实验成功完成后会生成以下文件：
- `tfdwt_detailed_results_YYYYMMDD_HHMMSS.csv` - 详细的fold结果
- `tfdwt_summary_stats_YYYYMMDD_HHMMSS.csv` - 汇总统计结果

## 重要注意事项

### 硬件要求
- **GPU**: 实验配置为 `device_mode: cuda`，需要CUDA兼容的GPU
- **内存**: 建议至少8GB RAM
- **存储**: 确保有足够的磁盘空间存储结果

### 数据要求
- 确保数据路径正确：
  - P3数据: `../P3_Raw_Data_BIDS-Compatible`
  - AVO数据: `../ds005863`

### 运行时间
- 完整的5-fold × 5-repeat交叉验证预计需要几小时到十几小时
- 具体时间取决于硬件性能

## 结果验证

### 核心指标对比
将生成的 `tfdwt_summary_stats_*.csv` 与目标文件对比：

| 指标 | 目标值 | 允许误差 |
|------|--------|----------|
| overall_accuracy_mean | 0.6297 | ±0.005 |
| overall_accuracy_std | 0.0255 | ±0.002 |
| p3_accuracy_mean | 0.5853 | ±0.005 |
| avo_accuracy_mean | 0.6415 | ±0.005 |

### 置信区间验证
| 指标 | 目标下界 | 目标上界 |
|------|----------|----------|
| overall_accuracy | 0.6192 | 0.6402 |
| p3_accuracy | 0.5702 | 0.6003 |
| avo_accuracy | 0.6298 | 0.6531 |

## 故障排除

### 常见问题

1. **CUDA错误**：
   - 检查GPU是否可用
   - 考虑修改 `DEVICE_MODE = 'cpu'` 进行CPU运行测试

2. **数据路径错误**：
   - 验证数据文件夹存在
   - 检查相对路径是否正确

3. **内存不足**：
   - 减小 `BATCH_SIZE` (如从32改为16)
   - 启用内存优化选项

4. **结果差异**：
   - 确认所有配置参数完全一致
   - 检查随机种子设置
   - 验证数据预处理一致性

### 配置验证
运行前可以验证关键配置：
```python
import config
print(f"electrode_list: {config.electrode_list}")
print(f"trials_per_subject_P3: {config.NESTED_CV_TRIALS_PER_SUBJECT_P3}")
print(f"trials_per_subject_AVO: {config.NESTED_CV_TRIALS_PER_SUBJECT_AVO}")
print(f"batch_size: {config.BATCH_SIZE}")
print(f"learning_rate: {config.LEARNING_RATE}")
```

## 与其他实验的比较

| 实验 | 总体准确率 | P3准确率 | AVO准确率 | 标准差 |
|------|------------|----------|-----------|--------|
| 20250924_204648 | 0.6389 | 0.5884 | 0.6523 | 0.0180 |
| 20250925_021227 | 0.6297 | 0.5853 | 0.6415 | 0.0255 |

此实验的准确率略低于20250924_204648实验，但标准差较大，表明结果的变异性更高。

## 文件说明

- `config_reproduce_tfdwt_20250925_021227.py`: 完整的复现配置文件
- `reproduce_tfdwt_20250925_021227.py`: 自动化复现脚本
- `REPRODUCE_TFDWT_20250925_021227.md`: 本说明文档

## 支持

如果复现过程中遇到问题：
1. 检查所有路径和依赖项
2. 验证硬件兼容性
3. 对比配置文件参数
4. 检查日志输出中的错误信息

成功复现的标志是获得与目标值相近的准确率和置信区间。