# 当前方法痛点分析

## 现状总结

### 目标
- **AVO场景** (P3=80, AVO=10): AVO准确率 ≥ 0.66，连续5次稳定
- **P3场景** (P3=10, AVO=80): P3准确率 ≥ 0.62，连续5次稳定

### 历史结果
| 实验 | AVO准确率 | P3准确率 | AVO达标 | 备注 |
|------|-----------|----------|---------|------|
| Round 4-1 | 0.6664 | 0.5858 | ✓ | 达标但P3很低 |
| Round 4-2 | 0.6515 | 0.5489 | ✗ | 差0.0085 |
| Round 5-1 | 0.6642 | 0.5423 | ✓ | 达标但P3更低 |
| **平均** | **0.6607** | **0.5590** | 2/3 | 波动0.0066 |

## 核心问题识别

### 问题1: 临界徘徊 (Critical)
- AVO平均0.6607，**仅高于目标0.0007**
- 3次测试中只有2次达标 (66.7%成功率)
- **结论**: 当前方法已接近极限，但**稳定性不足**

### 问题2: 大域性能崩溃
- 在优化小域(AVO)时，大域(P3)性能严重下降
- P3从baseline的~0.65降到0.54-0.59
- **这违反了迁移学习的基本原则**: 不应该严重伤害源域性能

### 问题3: 方法论瓶颈

#### 当前策略的局限性
```
核心思路: 通过权重(17x)和MMD(0.68)强制对齐
问题:
1. 过强的权重导致梯度不平衡
   - 小域loss被放大17倍
   - 优化器倾向于只满足小域，忽略大域

2. MMD对齐是"软约束"
   - 只对齐特征分布，不保证判别性
   - 可能把两个域对齐到无判别力的中间状态

3. Focal Loss的局限
   - 只关注hard examples
   - 10个trials中可能只有2-3个hard，其余信息浪费
```

## 深层次原因

### 数据流分析

```
训练流程:
1. 大域batch (P3: 80 trials) → forward → loss_large
2. 小域batch (AVO: 10 trials) → forward → loss_small (focal)
3. MMD对齐: align(features_large, features_small)
4. Total: w_large * loss_large + w_small * loss_small + λ * mmd

问题所在:
- Step 2: 10个trials的batch size=32，实际每个epoch只有0.3个batch
- 极少的样本导致梯度估计方差极大
- Focal loss进一步缩小有效样本(只关注hard的2-3个)
- 最终: 每个epoch只有1-2个样本真正指导优化
```

### 为什么Round 3崩溃但Round 4/5稳定？

**Round 3 (20x, MMD=0.8)**:
- 过强压制导致某些fold的P3特征完全被破坏
- AVO=0.5546的fold: P3特征被对齐到无效区域

**Round 4/5 (15-17x, MMD=0.65-0.68)**:
- 稍弱的压制保留了部分P3特征
- 但仍然不够强到让AVO稳定>0.66

**矛盾**:
- 再强会崩溃
- 再弱达不到目标
- **陷入两难境地**

## 根本矛盾

```
10 trials/subject 的根本限制:
- 信息熵不足
- 无法覆盖类内变异
- 难以学习robust特征

当前TF-DWT方法:
- 试图从80 trials学到的知识迁移到10 trials
- 但10 trials不足以验证迁移是否有效
- 导致过拟合到特定的10个样本
```

## 改进方向

### 方向1: 改善小样本学习 (推荐)
**核心思路**: 不要强行对齐，而是让小域"借用"大域的统计信息

1. **BatchNorm统计量共享**
   - 当前: 分离的BN buffers
   - 改进: 小域使用大域的BN统计量
   - 理由: 10个样本不足以估计准确的mean/var

2. **原型网络 (Prototypical Networks)**
   - 计算大域每个类的原型(均值)
   - 小域样本与原型距离作为额外监督
   - 避免直接对齐，保持判别性

3. **知识蒸馏**
   - 先在大域训练强teacher
   - 小域学习时蒸馏teacher的soft labels
   - 比hard labels包含更多信息

### 方向2: 正则化强化
1. **Mixup on small domain**
   - 10个样本做mixup可以生成更多虚拟样本
   - 平滑决策边界

2. **伪标签 (Pseudo-labeling)**
   - 用大域模型给小域未标注的trials打伪标签
   - 扩充小域训练集

### 方向3: 优化策略调整
1. **两阶段训练**
   - Stage 1: 只训练大域，获得强backbone
   - Stage 2: 冻结大部分参数，只微调classifier + 小域

2. **渐进式权重**
   - 当前: epoch线性warmup
   - 改进: 根据小域val performance自适应调整

## 选择方案

**优先级**:
1. **方向1.1 + 1.2**: BN共享 + 原型网络 (最直接有效)
2. **方向2.1**: Mixup (简单且proven)
3. **方向3.1**: 两阶段训练 (fallback方案)

开始实施方向1.1 + 1.2 + 2.1的组合。
